{
  "name": "myo-family-ui",
  "version": "0.2.0",
  "private": true,
  "description": "Shared vanilla-JS/CSS UI building blocks (connection cards, folder icon, spinner, tab bar) for the MYO family of sibling apps. Brand/color tokens are NOT shipped here — each consuming app defines its own values for the token contract documented in README.md.",
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./styles.css": "./src/styles.css"
  },
  "files": [
    "src"
  ]
}
